{
  "version": 2,
  "builds": [
    {
      "src": "artifacts/taxi-fleet/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/artifacts/taxi-fleet/dist/$1" }
  ]
}
